import { FunctionsController } from "./controllers/functions.controller";
import { HttpClientController } from "./controllers/http-client.controller";
import { SocketController } from "./controllers/socket.controller";
import { AuthController } from "./controllers/auth.controller";
import { CacheController } from "./controllers/cache.controller";
import { isNode } from "./utils/platform.util";
import { cacheAdapter } from "./factories/cache-adapter.factory";
import { httpAdapter } from "./factories/http-adapter.factory";
import { authAdapter } from "./factories/auth-adapter.factory";
import { getConfig } from './bfast';
var BfastFunctions = /** @class */ (function () {
    function BfastFunctions(appName, authController, httpClientController) {
        this.appName = appName;
        this.authController = authController;
        this.httpClientController = httpClientController;
    }
    BfastFunctions.prototype.init = function () {
        var config = getConfig();
        var authCache = new CacheController(this.appName, config.DEFAULT_CACHE_DB_BFAST, config.DEFAULT_CACHE_COLLECTION_USER, cacheAdapter(this.appName));
        var restController = new HttpClientController(this.appName, httpAdapter(config, this.appName));
        var authController = new AuthController(this.appName, authCache, authAdapter(config, this.appName));
        if (!this.authController) {
            this.authController = authController;
        }
        if (!this.httpClientController) {
            this.httpClientController = restController;
        }
    };
    /**
     * exec a http client request
     * @param path {string} function name
     */
    BfastFunctions.prototype.request = function (path) {
        this.init();
        return new FunctionsController(path, this.httpClientController, this.authController, this.appName);
    };
    /**
     * listen for a realtime event from a bfast::functions
     * @param eventName
     * @param onConnect {function} callback when connection established
     * @param onDisconnect {function} callback when connection terminated
     */
    BfastFunctions.prototype.event = function (eventName, onConnect, onDisconnect) {
        return new SocketController(eventName, this.appName, onConnect, onDisconnect);
    };
    BfastFunctions.prototype.onHttpRequest = function (path, handler) {
        if (isNode) {
            return {
                method: null,
                path: path,
                onRequest: handler
            };
        }
        else {
            throw 'Works In NodeJs Environment Only';
        }
    };
    BfastFunctions.prototype.onPostHttpRequest = function (path, handler) {
        if (isNode) {
            return {
                method: 'POST',
                path: path,
                onRequest: handler
            };
        }
        else {
            throw 'Works In NodeJs Environment Only';
        }
    };
    BfastFunctions.prototype.onPutHttpRequest = function (path, handler) {
        if (isNode) {
            return {
                method: 'PUT',
                path: path,
                onRequest: handler
            };
        }
        else {
            throw 'Works In NodeJs Environment Only';
        }
    };
    BfastFunctions.prototype.onDeleteHttpRequest = function (path, handler) {
        if (isNode) {
            return {
                method: 'DELETE',
                path: path,
                onRequest: handler
            };
        }
        else {
            throw 'Works In NodeJs Environment Only';
        }
    };
    BfastFunctions.prototype.onGetHttpRequest = function (path, handler) {
        if (isNode) {
            return {
                method: 'GET',
                path: path,
                onRequest: handler
            };
        }
        else {
            throw 'Works In NodeJs Environment Only';
        }
    };
    BfastFunctions.prototype.onEvent = function (path, handler) {
        if (isNode) {
            return {
                name: path,
                onEvent: handler
            };
        }
        else {
            throw 'Works In NodeJs Environment Only';
        }
    };
    BfastFunctions.prototype.onGuard = function (path, handler) {
        if (isNode) {
            return {
                path: path,
                onGuard: handler
            };
        }
        else {
            throw 'Works In NodeJs Environment Only';
        }
    };
    BfastFunctions.prototype.onJob = function (schedule, handler) {
        var defaultRule = { second: '*', minute: '*', month: '*', day: '*', dayOfWeek: '*', hour: '*' };
        Object.keys(schedule).forEach(function (key) {
            delete defaultRule[key];
        });
        Object.assign(schedule, defaultRule);
        // @ts-ignore
        var rule = Object.keys(schedule).map(function (x) { return schedule[x]; }).join(' ');
        if (isNode) {
            return {
                onJob: handler,
                rule: rule,
            };
        }
        else {
            throw 'Works In NodeJs Environment Only';
        }
    };
    return BfastFunctions;
}());
export { BfastFunctions };
