import { CacheAdapter } from "../adapters/cache.adapter";
import { SyncsModel } from "../models/syncs.model";
export declare class CacheController {
    private readonly appName;
    private readonly database;
    private readonly collection;
    private readonly cacheAdapter;
    constructor(appName: string, database: string, collection: string, cacheAdapter: CacheAdapter);
    keys(): Promise<string[]>;
    clearAll(): Promise<boolean>;
    get<T>(key: string, options?: {
        secure?: boolean;
    }): Promise<T | null>;
    getBulk<T>(keys: string[], options?: {
        secure?: boolean;
    }): Promise<T[] | null>;
    getAll(): Promise<Array<any>>;
    query(filter: (element: {
        [key: string]: any;
    }[]) => any): Promise<any>;
    set<T>(key: string, data: T, options?: {
        dtl?: number;
        secure?: boolean;
    }): Promise<T>;
    setBulk<T>(keys: string[], data: T[], options?: {
        dtl?: number;
        secure?: boolean;
    }): Promise<T[]>;
    remove(key: string, force?: boolean): Promise<boolean>;
    addSyncs(data: SyncsModel): Promise<any>;
    removeOneSyncs(key: string): Promise<any>;
    removeAllSyncs(): Promise<any>;
    getAllSyncs(): Promise<SyncsModel[]>;
    getOneSyncs(key: string): Promise<SyncsModel>;
    getSyncsKeys(): Promise<string[]>;
}
